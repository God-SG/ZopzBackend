

<ul class="nav nav-tabs" role="tablist">
    <li class="nav-item" role="presentation">
        <a class="nav-link active" data-bs-toggle="tab" href="#createUser" aria-selected="true" role="tab">Create</a>
    </li>
    <li class="nav-item" role="presentation">
        <a class="nav-link" data-bs-toggle="tab" href="#listUsers" aria-selected="false" role="tab" tabindex="-1"
           hx-trigger="click,refreshUsers from:body" hx-get="/dash/admin/users/htmx/list"
           hx-target="#listUsers">List</a>
    </li>
</ul>
<div id="myTabContent" class="tab-content">
    <div class="tab-pane fade active show" id="createUser" role="tabpanel" hx-trigger="load" hx-get="/dash/admin/users/htmx/create">
        <div class="spinner-border" role="status">
            <span class="visually-hidden">Loading...</span>
        </div>
    </div>
    <div class="tab-pane fade" id="listUsers" role="tabpanel">
        <div class="spinner-border" role="status">
            <span class="visually-hidden">Loading...</span>
        </div>
    </div>
</div>

<script>
    const tabLinks = document.querySelectorAll('#myTab a');

    tabLinks.forEach(link => {
        link.addEventListener('shown.bs.tab', function (event) {
            const activeTab = event.target; 
            const previousTab = event.relatedTarget; 

            console.log('Active Tab:', activeTab.id);
            console.log('Previous Tab:', previousTab.id);

            if (activeTab.id === 'listUsers-tab') {
                console.log('Refresh listUsers');
            }
        });
    });
</script>